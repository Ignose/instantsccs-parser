import { Item } from "kolmafia";
import { Delayed } from "libram";
type ItemQuantity = {
    item: Item;
    quantity: number;
};
export type ValueFunctions = {
    value: (item: Item | ItemQuantity, useHistorical?: boolean) => number;
    averageValue: (...items: (Item | ItemQuantity)[]) => number;
};
export declare function makeValue(options?: {
    quick: boolean;
    itemValues?: Map<Item, Delayed<number>>;
}): ValueFunctions;
export {};
